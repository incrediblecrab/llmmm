# Independent replication of Epicure

Generated by `tools/replicate.py`. Every number below is **recomputed from the
shipped artefacts** (embeddings + `modes.json` + explorer labels). No value is
taken from the paper's own result tables.

Tally: **PASS** 12 · **PARTIAL** 9 · **FAIL** 6 · **UNVERIFIABLE** 6

---

## Headline finding: the mode-coherence claim does not survive a like-for-like control

The model cards headline *"within-mode coherence 0.611 / 0.833 / 0.703 against a
random-pair baseline of 0.097 / 0.348 / 0.115"* — margins of ~0.5.

Those two numbers are **different statistics computed over different populations**:

| | statistic actually used | reproduces? |
|---|---|---|
| the *baseline* | mean **pairwise** cosine over all vocabulary pairs | exactly (0.098 / 0.348 / 0.116) |
| the *coherence* | mean cosine **to the mode centroid**, over only the **~7 members closest to the pole** | 0.644 / 0.831 / 0.694 |

Mean-cosine-to-centroid is arithmetically always larger than mean pairwise cosine
over the same set, and restricting to a mode's 7 tightest members inflates it
again. Measured consistently, the same modes score **0.414 / 0.656 / 0.454**, and
mean pairwise over all members is only **0.162 / 0.424 / 0.198**.

Running the correct control — random ingredient groups **matched on mode size**,
scored with the **same** statistic:

| sibling | real modes | size-matched random | true margin | claimed margin | inflation |
|---|---|---|---|---|---|
| cooc | 0.414 | 0.330 | **+0.083** | 0.514 | 6.2x |
| core | 0.656 | 0.599 | **+0.057** | 0.485 | 8.5x |
| chem | 0.454 | 0.362 | **+0.093** | 0.588 | 6.4x |

**The modes are real but weak.** They are more coherent than chance, consistently
and in the same rank order the paper reports — but by ~0.06-0.09, not ~0.5.

### Why this matters for the product

Modes have a **tight core and a nearly-random tail** (median 89 members, max 254).
The top ~7 members are genuinely coherent; membership past ~20 is close to noise.

> **Design rule: never render a mode's full membership. Show the top 8 by pole
> proximity and stop.** Anything else exposes the tail and the feature will read
> as broken. This also means "browse this mode" is a weak screen — modes are
> better as *labels* and *steering targets* than as *lists*.

---

## Second finding: aggregated poles collapse onto the corpus mean direction

Every supervised "direction" built by averaging mode poles ends up ~0.95 aligned
with the global mean of the embedding matrix. Measured mean pairwise |cos|:

| sibling | sensory axes (raw) | sensory (centred) | cuisine (raw) | cos(pole, corpus mean) |
|---|---|---|---|---|
| cooc | **0.965** | 0.337 | 0.698 | 0.977 |
| core | **0.982** | 0.524 | 0.812 | 0.981 |
| chem | **0.939** | 0.374 | 0.708 | 0.954 |

At 0.94–0.98 the ten sensory axes are effectively **one vector**. Raw top-K is the
same list for every axis — corpus hubs:

```
cooc  sweet  -> black_pepper, garlic, grapeseed_oil, salt, lemon
cooc  citrus -> black_pepper, garlic, salt, cayenne_pepper, bay_leaf
cooc  bitter -> black_pepper, garlic, salt, cayenne_pepper, grapeseed_oil
```

Mean-centring drops collinearity to ~0.34 and makes results vivid — but the axis
*labels* still don't hold (`bitter` returns orange and lime; `citrus` returns
rosemary and thyme). **Verdict: do not ship labelled sensory sliders.**

Rule for the app: **single poles raw, aggregated poles centred**, and always
eyeball a new aggregate before putting it in the UI.

---

## Third finding: the SLERP table is 81% reconstructible and still doesn't reproduce

The `epicure-explorer` Space source (`app.py::_aggregate_pole`) reveals how the
author builds the missing `sweet` / `umami` / `high protein` directions. Applying
that recipe lifts coverage of `direction_arithmetic_full.parquet` from **10% → 81.2%**.
Agreement with the published table (`tools/slerp_via_space_recipe.py`):

| θ | mean top-5 overlap | exact top-5 |
|---|---|---|
| 0° | 94.4% | 76.9% |
| 30° | 34.5% | **0%** |
| 60° | 9.4% | **0%** |

θ=0 doesn't use the pole at all — it is pure `neighbors`, and it reproduces. Agreement
then decays monotonically with rotation, which is the signature of a **wrong pole**:
the shipped `supervised_poles.json` are not the vectors behind the published table.

Centring is *not* the missing step — it slightly helps θ>0 (0.345→0.368, 0.094→0.140)
but badly hurts θ=0 (0.944→0.786), so the paper used raw cosine.

`floral`, `nutty` and `high water` match no pole family at all. The other 9 uncovered
cells are exactly the §3 cuisine asymmetry — `Eastern_European` missing from cooc+chem
(6 cells) and `Japanese` missing from core (3) — an independent confirmation.

---

## Full log

| Claim | Verdict | Detail |
|---|---|---|
| cooc: participation ratio (published 173.6) | **PASS** | computed PR=178.6 (+2.9%), avg pairwise cos=0.098 |
| core: participation ratio (published 94.2) | **PASS** | computed PR=96.1 (+2.0%), avg pairwise cos=0.348 |
| chem: participation ratio (published 183.1) | **PASS** | computed PR=186.5 (+1.9%), avg pairwise cos=0.116 |
| cooc: published baseline 0.097 | **PASS** | baseline IS all-pairs mean cosine: computed 0.098 |
| cooc: published coherence 0.611 | **PASS** | reproduces ONLY as mean-cos-to-centroid over top-7 members: 0.644. Same statistic over ALL members = 0.414; mean *pairwise* over all members = 0.162 |
| cooc: claimed margin 0.514 survives a like-for-like control | **FAIL** | size-matched random groups scored 0.330 on the same statistic vs 0.414 for real modes -> TRUE margin +0.083, 6.2x smaller than claimed |
| core: published baseline 0.348 | **PASS** | baseline IS all-pairs mean cosine: computed 0.348 |
| core: published coherence 0.833 | **PASS** | reproduces ONLY as mean-cos-to-centroid over top-7 members: 0.831. Same statistic over ALL members = 0.656; mean *pairwise* over all members = 0.424 |
| core: claimed margin 0.485 survives a like-for-like control | **FAIL** | size-matched random groups scored 0.599 on the same statistic vs 0.656 for real modes -> TRUE margin +0.057, 8.5x smaller than claimed |
| chem: published baseline 0.115 | **PASS** | baseline IS all-pairs mean cosine: computed 0.116 |
| chem: published coherence 0.703 | **PASS** | reproduces ONLY as mean-cos-to-centroid over top-7 members: 0.694. Same statistic over ALL members = 0.454; mean *pairwise* over all members = 0.198 |
| chem: claimed margin 0.588 survives a like-for-like control | **FAIL** | size-matched random groups scored 0.362 on the same statistic vs 0.454 for real modes -> TRUE margin +0.093, 6.4x smaller than claimed |
| cooc: GMM-vs-foodgroup NMI (paper cooc band 0.2-0.25) | **PARTIAL** | computed NMI=0.115 on 8-group proxy — not directly comparable (different taxonomy + different clustering protocol) |
| core: GMM-vs-foodgroup NMI (paper cooc band 0.2-0.25) | **PARTIAL** | computed NMI=0.109 on 8-group proxy — not directly comparable (different taxonomy + different clustering protocol) |
| chem: GMM-vs-foodgroup NMI (paper cooc band 0.2-0.25) | **PARTIAL** | computed NMI=0.146 on 8-group proxy — not directly comparable (different taxonomy + different clustering protocol) |
| cooc: 20 ICA factors, multi-seed stable | **PARTIAL** | shipped atlas has 15 factor properties (expect 20); our 3-seed mean best-match |cos| = 1.000 |
| core: 20 ICA factors, multi-seed stable | **PARTIAL** | shipped atlas has 18 factor properties (expect 20); our 3-seed mean best-match |cos| = 1.000 |
| chem: 20 ICA factors, multi-seed stable | **PARTIAL** | shipped atlas has 17 factor properties (expect 20); our 3-seed mean best-match |cos| = 0.945 |
| cooc: UMAP kNN(15) overlap with 300-D neighbourhoods | **PASS** | overlap=0.246 (UMAP preserves local structure; low absolute values are normal for 300->2D) |
| core: UMAP kNN(15) overlap with 300-D neighbourhoods | **PASS** | overlap=0.304 (UMAP preserves local structure; low absolute values are normal for 300->2D) |
| chem: UMAP kNN(15) overlap with 300-D neighbourhoods | **PASS** | overlap=0.373 (UMAP preserves local structure; low absolute values are normal for 300->2D) |
| cooc: sensory poles are mutually distinguishable | **FAIL** | mean pairwise |cos| raw=0.965 (centred=0.337); mean cos(pole, corpus mean)=0.977 -> aggregated poles regress onto the global mean; centre before scoring |
| cooc: cuisine poles are mutually distinguishable | **PARTIAL** | mean pairwise |cos| raw=0.698 (centred=0.399); mean cos(pole, corpus mean)=0.812 -> aggregated poles regress onto the global mean; centre before scoring |
| core: sensory poles are mutually distinguishable | **FAIL** | mean pairwise |cos| raw=0.982 (centred=0.524); mean cos(pole, corpus mean)=0.981 -> aggregated poles regress onto the global mean; centre before scoring |
| core: cuisine poles are mutually distinguishable | **PARTIAL** | mean pairwise |cos| raw=0.812 (centred=0.323); mean cos(pole, corpus mean)=0.894 -> aggregated poles regress onto the global mean; centre before scoring |
| chem: sensory poles are mutually distinguishable | **FAIL** | mean pairwise |cos| raw=0.939 (centred=0.374); mean cos(pole, corpus mean)=0.954 -> aggregated poles regress onto the global mean; centre before scoring |
| chem: cuisine poles are mutually distinguishable | **PARTIAL** | mean pairwise |cos| raw=0.708 (centred=0.293); mean cos(pole, corpus mean)=0.826 -> aggregated poles regress onto the global mean; centre before scoring |
| Direction quality: 5-fold CV Spearman rho (0.28/0.40/0.42 etc.) | **UNVERIFIABLE** | per-ingredient continuous sensory + USDA labels are NOT published |
| Cuisine separability: Cohen's d 2.43/2.70/3.07 | **UNVERIFIABLE** | per-ingredient 8-region cuisine tags are NOT published (authors confirm) |
| Soft NMI vs cuisine macro-regions (0.43-0.46) | **UNVERIFIABLE** | same missing cuisine tags |
| WEAT effect sizes (8 tests) | **UNVERIFIABLE** | target/attribute word sets are not published; only results table ships |
| Section 4.2 SLERP hero tables | **UNVERIFIABLE** | reconstructible to 81% via the Space's _aggregate_pole recipe, but 0% exact at any theta>0 and overlap decays 94%->35%->9% with angle -- shipped poles are not the published ones (see FINDINGS.md 4) |
| Any corpus/graph statistic (4.14M recipes, 203,508 NPMI edges) | **UNVERIFIABLE** | raw corpus and both graphs are explicitly withheld |
