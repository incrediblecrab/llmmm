# epicure-explorer — full API reference

Auto-generated by `tools/parse_api_docs.py` from
`data/raw/epicure-explorer/gradio_api_docs.txt`
(captured from <https://kaikaku-epicure-explorer.hf.space/?view=api>).

See `docs/EXPLORER_API.md` for what this API is useful for — in short, it is a
**bit-exact conformance oracle** for the Swift port (100% parity over 54 calls),
but it does **not** reproduce the paper's published tables.

## Calling it without the `gradio_client` dependency

Two steps: POST to get an `event_id`, then GET the SSE stream.

```bash
BASE=https://kaikaku-epicure-explorer.hf.space/gradio_api/call
EID=$(curl -s -X POST "$BASE/neighbors" -H "Content-Type: application/json" \
  -d '{"data":["miso","core",5]}' | sed -n 's/.*"event_id":"\([^"]*\)".*/\1/p')
curl -s "$BASE/neighbors/$EID"
# event: complete
# data: [[{"name":"mirin","cosine":0.788707}, ...]]
```

`tools/api_differential.py` implements this with retries and an on-disk cache.

## ⚠️ Only 4 of these 34 endpoints are a real API

Gradio exposes *every* event handler as an endpoint, so the list below
conflates two very different things:

- **4 programmatic endpoints** (`gr.api(...)`) — `/neighbors`, `/slerp`,
  `/arithmetic`, `/embed`. Clean JSON in, clean JSON out. These are a
  deliberate contract and are what `tools/api_differential.py` targets.
- **30 UI event handlers** — incidental exposure of the Gradio UI wiring.
  They return rendered HTML or plot payloads, their arguments are ordered
  by widget layout, and they will break on any UI edit. Do not depend on them.

The tell: a real API endpoint has no UI component, so Gradio falls back to
positional placeholder labels (`parameter_217`). A UI handler carries the
human label ("Ingredient basket"). `is_programmatic()` uses exactly this.

**Arguments are positional over raw HTTP.** The `data` array must follow the
parameter order in the tables below — e.g. `/slerp` is
`[seed, direction, theta_deg, sibling, k]`, with `sibling` *after* `theta_deg`.
That ordering is empirically confirmed: it is what produced 100% parity.

## Endpoint index (34)

| endpoint | params | returns | purpose |
|---|---|---|---|
| `/_filter_dropdown` | 2 | 1 | Filter dropdown by food group |
| `/browse_modes` | 3 | 1 | Sibling |
| `/explore_all_siblings` | 2 | 0 | Ingredient basket |
| `/lambda` | 1 | 1 | Sibling |
| `/lambda_1` | 1 | 1 | Sibling |
| `/transform` | 8 | 0 | Sibling |
| `/umap_view` | 5 | 1 | Sibling |
| `/parse_or_suggest` | 3 | 0 | Free text |
| `/lambda_2` | 0 | 1 | Ingredient basket |
| `/substitute_finder` | 6 | 0 | Seed ingredient |
| `/sensory_search` | 12 | 0 | Sibling |
| `/_passport_outputs` | 1 | 0 | Ingredient |
| `/_passport_outputs_1` | 1 | 0 | Ingredient |
| `/lambda_3` | 1 | 1 | Sibling |
| `/render_mode_wiki` | 2 | 1 | Sibling |
| `/cultural_context` | 3 | 0 | Ingredient |
| `/cultural_context_1` | 3 | 0 | Ingredient |
| `/render_3d_atlas` | 5 | 1 | Sibling |
| `/render_3d_atlas_1` | 5 | 1 | Sibling |
| `/render_3d_atlas_2` | 5 | 1 | Sibling |
| `/render_3d_atlas_3` | 5 | 1 | Sibling |
| `/render_factor_poster` | 2 | 1 | Sibling |
| `/_refresh_factor_choices` | 1 | 1 | Sibling |
| `/render_factor_poster_1` | 2 | 1 | Sibling |
| `/render_cuisine_compass` | 2 | 1 | Sibling |
| `/render_arithmetic_vector` | 3 | 1 | Sibling |
| `/render_cuisine_phylogeny` | 1 | 1 | Sibling |
| `/render_cuisine_cosine_map` | 1 | 1 | Sibling |
| `/render_slerp_trajectory` | 4 | 1 | Sibling |
| `/lambda_4` | 1 | 1 | Sibling |
| `/neighbors` ⭐ | 3 | 1 | parameter_217 |
| `/slerp` ⭐ | 5 | 1 | parameter_217 |
| `/arithmetic` ⭐ | 4 | 1 | parameter_221 |
| `/embed` ⭐ | 2 | 1 | parameter_217 |

⭐ = stable programmatic endpoint. Unstarred rows are UI event handlers.

---

## Endpoints

### `/_filter_dropdown`

```python
client = Client("Kaikaku/epicure-explorer")
result = client.predict(
    group="All",
    current_value=["chicken","lemon","garlic"],
    api_name="/_filter_dropdown"
)
```

**Parameters (2)**

| name | type | default | UI label | component |
|---|---|---|---|---|
| `group` | `Literal['All', 'Vegetable', 'Spice', 'Fruit', 'Dairy', 'Grain', 'Pantry', 'Beverage', 'Other']` | `"All"` | Filter dropdown by food group | Radio |
| `current_value` | `List[Literal['abalone', 'abalone_mushroom', 'absinthe', ... (1790 values)]]` | `["chicken","lemon","garlic"]` | Ingredient basket | Dropdown |

**Returns (1)**

| type | UI label | component |
|---|---|---|
| `List[Literal['abalone', 'abalone_mushroom', 'absinthe', ... (1790 values)]]` | Ingredient basket | Dropdown |

### `/browse_modes`

```python
client = Client("Kaikaku/epicure-explorer")
result = client.predict(
    sibling="chem",
    kind_filter="all",
    query="Hello!!",
    api_name="/browse_modes"
)
```

**Parameters (3)**

| name | type | default | UI label | component |
|---|---|---|---|---|
| `sibling` | `Literal['cooc', 'core', 'chem']` | `"chem"` | Sibling | Radio |
| `kind_filter` | `Literal['all', 'factor', 'continuous', 'binary']` | `"all"` | Kind | Radio |
| `query` | `str Required` | — | Search labels | Textbox |

**Returns (1)**

| type | UI label | component |
|---|---|---|
| `Dict(headers: List[str], data: List[List[Any]], metadata: Dict(str, List[Any] | None) | None)` | value_33 | Dataframe |

### `/explore_all_siblings`

```python
client = Client("Kaikaku/epicure-explorer")
result = client.predict(
    basket=["chicken","lemon","garlic"],
    k=8,
    api_name="/explore_all_siblings"
)
```

**Parameters (2)**

| name | type | default | UI label | component |
|---|---|---|---|---|
| `basket` | `List[Literal['abalone', 'abalone_mushroom', 'absinthe', ... (1790 values)]]` | `["chicken","lemon","garlic"]` | Ingredient basket | Dropdown |
| `k` | `float` | `8` | K | Slider |

### `/lambda`

```python
client = Client("Kaikaku/epicure-explorer")
result = client.predict(
    s="core",
    api_name="/lambda"
)
```

**Parameters (1)**

| name | type | default | UI label | component |
|---|---|---|---|---|
| `s` | `Literal['cooc', 'core', 'chem']` | `"core"` | Sibling | Radio |

**Returns (1)**

| type | UI label | component |
|---|---|---|
| `List[Literal['bitter_score/Aromatic bitter spices and herbs', 'bitter_score/East Asian pungent aromatics and mushrooms', 'bitter_score/Mediterranean aged cheeses and mushrooms', ... (123 values)]]` | Supervised directions (for 'Rotate to supervised') | Dropdown |

### `/lambda_1`

```python
client = Client("Kaikaku/epicure-explorer")
result = client.predict(
    s="core",
    api_name="/lambda_1"
)
```

**Parameters (1)**

| name | type | default | UI label | component |
|---|---|---|---|---|
| `s` | `Literal['cooc', 'core', 'chem']` | `"core"` | Sibling | Radio |

**Returns (1)**

| type | UI label | component |
|---|---|---|
| `List[Literal['Mediterranean savory vegetable stews (F_0/M0)', 'East Asian fish and savory fermented pastes (F_0/M1)', 'East Asian vegetables and mushrooms (F_0/M2)', ... (89 values)]]` | Factor modes (for 'Rotate to emergent') | Dropdown |

### `/transform`

```python
client = Client("Kaikaku/epicure-explorer")
result = client.predict(
    sibling="core",
    op="Arithmetic (basket - negatives)",
    basket=["miso"],
    directions=[],
    mode_labels=[],
    theta=30,
    negatives=["salt"],
    k=8,
    api_name="/transform"
)
```

**Parameters (8)**

| name | type | default | UI label | component |
|---|---|---|---|---|
| `sibling` | `Literal['cooc', 'core', 'chem']` | `"core"` | Sibling | Radio |
| `op` | `Literal['Rotate to supervised direction', 'Rotate to emergent mode', 'Arithmetic (basket - negatives)']` | `"Arithmetic (basket - negatives)"` | Operation | Radio |
| `basket` | `List[Literal['abalone', 'abalone_mushroom', 'absinthe', ... (1790 values)]]` | `["miso"]` | Basket / positives | Dropdown |
| `directions` | `List[Literal['bitter_score/Aromatic bitter spices and herbs', 'bitter_score/East Asian pungent aromatics and mushrooms', 'bitter_score/Mediterranean aged cheeses and mushrooms', ... (123 values)]]` | `[]` | Supervised directions (for 'Rotate to supervised') | Dropdown |
| `mode_labels` | `List[Literal['Mediterranean savory vegetable stews (F_0/M0)', 'East Asian fish and savory fermented pastes (F_0/M1)', 'East Asian vegetables and mushrooms (F_0/M2)', ... (89 values)]]` | `[]` | Factor modes (for 'Rotate to emergent') | Dropdown |
| `theta` | `float` | `30` | Rotation angle (deg, SLERP only) | Slider |
| `negatives` | `List[Literal['abalone', 'abalone_mushroom', 'absinthe', ... (1790 values)]]` | `["salt"]` | Negatives (Arithmetic only) | Dropdown |
| `k` | `float` | `8` | K | Slider |

### `/umap_view`

```python
client = Client("Kaikaku/epicure-explorer")
result = client.predict(
    sibling="chem",
    basket=["chicken","lemon","garlic"],
    show_neighbours=True,
    k=10,
    three_d=False,
    api_name="/umap_view"
)
```

**Parameters (5)**

| name | type | default | UI label | component |
|---|---|---|---|---|
| `sibling` | `Literal['cooc', 'core', 'chem']` | `"chem"` | Sibling | Radio |
| `basket` | `List[Literal['abalone', 'abalone_mushroom', 'absinthe', ... (1790 values)]]` | `["chicken","lemon","garlic"]` | Highlight basket | Dropdown |
| `show_neighbours` | `bool` | `True` | Show top-K neighbours | Checkbox |
| `k` | `float` | `10` | K | Slider |
| `three_d` | `bool` | `False` | 3-D | Checkbox |

**Returns (1)**

| type | UI label | component |
|---|---|---|
| `Dict(type: Literal['altair', 'bokeh', 'plotly', 'matplotlib'], plot: str)` | UMAP | Plot |

### `/parse_or_suggest`

```python
client = Client("Kaikaku/epicure-explorer")
result = client.predict(
    text="I'm making Thai green curry for 4 people",
    sibling="chem",
    mode_choice="Recipe / dish description",
    api_name="/parse_or_suggest"
)
```

**Parameters (3)**

| name | type | default | UI label | component |
|---|---|---|---|---|
| `text` | `str` | `"I'm making Thai green curry for 4 people"` | Free text | Textbox |
| `sibling` | `Literal['cooc', 'core', 'chem']` | `"chem"` | Sibling | Radio |
| `mode_choice` | `Literal['Recipe / dish description', 'Ingredient list (shopping list / fridge)']` | `"Recipe / dish description"` | Treat as | Radio |

### `/lambda_2`
**Returns (1)**

| type | UI label | component |
|---|---|---|
| `List[Literal['abalone', 'abalone_mushroom', 'absinthe', ... (1790 values)]]` | Ingredient basket | Dropdown |

### `/substitute_finder`

```python
client = Client("Kaikaku/epicure-explorer")
result = client.predict(
    seed="mascarpone_cheese",
    sibling="chem",
    k=10,
    must_share_group=True,
    same_nova=False,
    diff_cuisine=False,
    api_name="/substitute_finder"
)
```

**Parameters (6)**

| name | type | default | UI label | component |
|---|---|---|---|---|
| `seed` | `Literal['abalone', 'abalone_mushroom', 'absinthe', ... (1790 values)]` | `"mascarpone_cheese"` | Seed ingredient | Dropdown |
| `sibling` | `Literal['cooc', 'core', 'chem']` | `"chem"` | Sibling | Dropdown |
| `k` | `float` | `10` | K | Slider |
| `must_share_group` | `bool` | `True` | Must share food group | Checkbox |
| `same_nova` | `bool` | `False` | Pull toward same NOVA level | Checkbox |
| `diff_cuisine` | `bool` | `False` | Rotate 30° from dominant cuisine | Checkbox |

### `/sensory_search`

```python
client = Client("Kaikaku/epicure-explorer")
result = client.predict(
    sibling="chem",
    k=15,
    param_2=0,
    param_3=0,
    param_4=0,
    param_5=0,
    param_6=0,
    param_7=0,
    param_8=0,
    param_9=0,
    param_10=0,
    param_11=0,
    api_name="/sensory_search"
)
```

**Parameters (12)**

| name | type | default | UI label | component |
|---|---|---|---|---|
| `sibling` | `Literal['cooc', 'core', 'chem']` | `"chem"` | Sibling | Dropdown |
| `k` | `float` | `15` | K | Slider |
| `param_2` | `float` | `0` | sweet | Slider |
| `param_3` | `float` | `0` | sour | Slider |
| `param_4` | `float` | `0` | bitter | Slider |
| `param_5` | `float` | `0` | umami | Slider |
| `param_6` | `float` | `0` | fatty | Slider |
| `param_7` | `float` | `0` | pungent | Slider |
| `param_8` | `float` | `0` | savory | Slider |
| `param_9` | `float` | `0` | citrus | Slider |
| `param_10` | `float` | `0` | woody | Slider |
| `param_11` | `float` | `0` | earthy | Slider |

### `/_passport_outputs`

```python
client = Client("Kaikaku/epicure-explorer")
result = client.predict(
    name="basil",
    api_name="/_passport_outputs"
)
```

**Parameters (1)**

| name | type | default | UI label | component |
|---|---|---|---|---|
| `name` | `Literal['abalone', 'abalone_mushroom', 'absinthe', ... (1790 values)]` | `"basil"` | Ingredient | Dropdown |

### `/_passport_outputs_1`

```python
client = Client("Kaikaku/epicure-explorer")
result = client.predict(
    name="basil",
    api_name="/_passport_outputs_1"
)
```

**Parameters (1)**

| name | type | default | UI label | component |
|---|---|---|---|---|
| `name` | `Literal['abalone', 'abalone_mushroom', 'absinthe', ... (1790 values)]` | `"basil"` | Ingredient | Dropdown |

### `/lambda_3`

```python
client = Client("Kaikaku/epicure-explorer")
result = client.predict(
    s="chem",
    api_name="/lambda_3"
)
```

**Parameters (1)**

| name | type | default | UI label | component |
|---|---|---|---|---|
| `s` | `Literal['cooc', 'core', 'chem']` | `"chem"` | Sibling | Radio |

**Returns (1)**

| type | UI label | component |
|---|---|---|
| `Literal['fg_Fruit/M0', 'fg_Grain/M1', 'fg_Pantry/M0', ... (200 values)]` | Mode | Dropdown |

### `/render_mode_wiki`

```python
client = Client("Kaikaku/epicure-explorer")
result = client.predict(
    sibling="chem",
    mode_id="fg_Fruit/M0",
    api_name="/render_mode_wiki"
)
```

**Parameters (2)**

| name | type | default | UI label | component |
|---|---|---|---|---|
| `sibling` | `Literal['cooc', 'core', 'chem']` | `"chem"` | Sibling | Radio |
| `mode_id` | `Literal['fg_Fruit/M0', 'fg_Grain/M1', 'fg_Pantry/M0', ... (200 values)]` | `"fg_Fruit/M0"` | Mode | Dropdown |

**Returns (1)**

| type | UI label | component |
|---|---|---|
| `str` | value_144 | Markdown |

### `/cultural_context`

```python
client = Client("Kaikaku/epicure-explorer")
result = client.predict(
    ingredient="gochujang",
    sibling="chem",
    k=4,
    api_name="/cultural_context"
)
```

**Parameters (3)**

| name | type | default | UI label | component |
|---|---|---|---|---|
| `ingredient` | `Literal['abalone', 'abalone_mushroom', 'absinthe', ... (1790 values)]` | `"gochujang"` | Ingredient | Dropdown |
| `sibling` | `Literal['cooc', 'core', 'chem']` | `"chem"` | Sibling | Radio |
| `k` | `float` | `4` | Top-K cuisines | Slider |

### `/cultural_context_1`

```python
client = Client("Kaikaku/epicure-explorer")
result = client.predict(
    ingredient="gochujang",
    sibling="chem",
    k=4,
    api_name="/cultural_context_1"
)
```

**Parameters (3)**

| name | type | default | UI label | component |
|---|---|---|---|---|
| `ingredient` | `Literal['abalone', 'abalone_mushroom', 'absinthe', ... (1790 values)]` | `"gochujang"` | Ingredient | Dropdown |
| `sibling` | `Literal['cooc', 'core', 'chem']` | `"chem"` | Sibling | Radio |
| `k` | `float` | `4` | Top-K cuisines | Slider |

### `/render_3d_atlas`

```python
client = Client("Kaikaku/epicure-explorer")
result = client.predict(
    sibling="chem",
    basket=["miso","basil","chocolate","tomato"],
    show_neighbours=True,
    k=8,
    color_mode="food group",
    api_name="/render_3d_atlas"
)
```

**Parameters (5)**

| name | type | default | UI label | component |
|---|---|---|---|---|
| `sibling` | `Literal['cooc', 'core', 'chem']` | `"chem"` | Sibling | Radio |
| `basket` | `List[Literal['abalone', 'abalone_mushroom', 'absinthe', ... (1790 values)]]` | `["miso","basil","chocolate","tomato"]` | Highlight these ingredients | Dropdown |
| `show_neighbours` | `bool` | `True` | Show top-K neighbours | Checkbox |
| `k` | `float` | `8` | K | Slider |
| `color_mode` | `Literal['food group', 'cuisine']` | `"food group"` | Color points by | Radio |

**Returns (1)**

| type | UI label | component |
|---|---|---|
| `Dict(type: Literal['altair', 'bokeh', 'plotly', 'matplotlib'], plot: str)` |  | Plot |

### `/render_3d_atlas_1`

```python
client = Client("Kaikaku/epicure-explorer")
result = client.predict(
    sibling="chem",
    basket=["miso","basil","chocolate","tomato"],
    show_neighbours=True,
    k=8,
    color_mode="food group",
    api_name="/render_3d_atlas_1"
)
```

**Parameters (5)**

| name | type | default | UI label | component |
|---|---|---|---|---|
| `sibling` | `Literal['cooc', 'core', 'chem']` | `"chem"` | Sibling | Radio |
| `basket` | `List[Literal['abalone', 'abalone_mushroom', 'absinthe', ... (1790 values)]]` | `["miso","basil","chocolate","tomato"]` | Highlight these ingredients | Dropdown |
| `show_neighbours` | `bool` | `True` | Show top-K neighbours | Checkbox |
| `k` | `float` | `8` | K | Slider |
| `color_mode` | `Literal['food group', 'cuisine']` | `"food group"` | Color points by | Radio |

**Returns (1)**

| type | UI label | component |
|---|---|---|
| `Dict(type: Literal['altair', 'bokeh', 'plotly', 'matplotlib'], plot: str)` |  | Plot |

### `/render_3d_atlas_2`

```python
client = Client("Kaikaku/epicure-explorer")
result = client.predict(
    sibling="chem",
    basket=["miso","basil","chocolate","tomato"],
    show_neighbours=True,
    k=8,
    color_mode="food group",
    api_name="/render_3d_atlas_2"
)
```

**Parameters (5)**

| name | type | default | UI label | component |
|---|---|---|---|---|
| `sibling` | `Literal['cooc', 'core', 'chem']` | `"chem"` | Sibling | Radio |
| `basket` | `List[Literal['abalone', 'abalone_mushroom', 'absinthe', ... (1790 values)]]` | `["miso","basil","chocolate","tomato"]` | Highlight these ingredients | Dropdown |
| `show_neighbours` | `bool` | `True` | Show top-K neighbours | Checkbox |
| `k` | `float` | `8` | K | Slider |
| `color_mode` | `Literal['food group', 'cuisine']` | `"food group"` | Color points by | Radio |

**Returns (1)**

| type | UI label | component |
|---|---|---|
| `Dict(type: Literal['altair', 'bokeh', 'plotly', 'matplotlib'], plot: str)` |  | Plot |

### `/render_3d_atlas_3`

```python
client = Client("Kaikaku/epicure-explorer")
result = client.predict(
    sibling="chem",
    basket=["miso","basil","chocolate","tomato"],
    show_neighbours=True,
    k=8,
    color_mode="food group",
    api_name="/render_3d_atlas_3"
)
```

**Parameters (5)**

| name | type | default | UI label | component |
|---|---|---|---|---|
| `sibling` | `Literal['cooc', 'core', 'chem']` | `"chem"` | Sibling | Radio |
| `basket` | `List[Literal['abalone', 'abalone_mushroom', 'absinthe', ... (1790 values)]]` | `["miso","basil","chocolate","tomato"]` | Highlight these ingredients | Dropdown |
| `show_neighbours` | `bool` | `True` | Show top-K neighbours | Checkbox |
| `k` | `float` | `8` | K | Slider |
| `color_mode` | `Literal['food group', 'cuisine']` | `"food group"` | Color points by | Radio |

**Returns (1)**

| type | UI label | component |
|---|---|---|
| `Dict(type: Literal['altair', 'bokeh', 'plotly', 'matplotlib'], plot: str)` |  | Plot |

### `/render_factor_poster`

```python
client = Client("Kaikaku/epicure-explorer")
result = client.predict(
    sibling="chem",
    factor_id="F_0",
    api_name="/render_factor_poster"
)
```

**Parameters (2)**

| name | type | default | UI label | component |
|---|---|---|---|---|
| `sibling` | `Literal['cooc', 'core', 'chem']` | `"chem"` | Sibling | Radio |
| `factor_id` | `Literal['F_0', 'F_1', 'F_2', 'F_3', 'F_4', 'F_5', 'F_6', 'F_7', 'F_8', 'F_9', 'F_10', 'F_11', 'F_12', 'F_13', 'F_14', 'F_18', 'F_19']` | `"F_0"` | Factor (preview of mode labels shown) | Dropdown |

**Returns (1)**

| type | UI label | component |
|---|---|---|
| `Dict(type: Literal['altair', 'bokeh', 'plotly', 'matplotlib'], plot: str)` |  | Plot |

### `/_refresh_factor_choices`

```python
client = Client("Kaikaku/epicure-explorer")
result = client.predict(
    s="chem",
    api_name="/_refresh_factor_choices"
)
```

**Parameters (1)**

| name | type | default | UI label | component |
|---|---|---|---|---|
| `s` | `Literal['cooc', 'core', 'chem']` | `"chem"` | Sibling | Radio |

**Returns (1)**

| type | UI label | component |
|---|---|---|
| `Literal['F_0', 'F_1', 'F_2', 'F_3', 'F_4', 'F_5', 'F_6', 'F_7', 'F_8', 'F_9', 'F_10', 'F_11', 'F_12', 'F_13', 'F_14', 'F_18', 'F_19']` | Factor (preview of mode labels shown) | Dropdown |

### `/render_factor_poster_1`

```python
client = Client("Kaikaku/epicure-explorer")
result = client.predict(
    sibling="chem",
    factor_id="F_0",
    api_name="/render_factor_poster_1"
)
```

**Parameters (2)**

| name | type | default | UI label | component |
|---|---|---|---|---|
| `sibling` | `Literal['cooc', 'core', 'chem']` | `"chem"` | Sibling | Radio |
| `factor_id` | `Literal['F_0', 'F_1', 'F_2', 'F_3', 'F_4', 'F_5', 'F_6', 'F_7', 'F_8', 'F_9', 'F_10', 'F_11', 'F_12', 'F_13', 'F_14', 'F_18', 'F_19']` | `"F_0"` | Factor (preview of mode labels shown) | Dropdown |

**Returns (1)**

| type | UI label | component |
|---|---|---|
| `Dict(type: Literal['altair', 'bokeh', 'plotly', 'matplotlib'], plot: str)` |  | Plot |

### `/render_cuisine_compass`

```python
client = Client("Kaikaku/epicure-explorer")
result = client.predict(
    sibling="chem",
    ingredients=["miso","basil","cumin"],
    api_name="/render_cuisine_compass"
)
```

**Parameters (2)**

| name | type | default | UI label | component |
|---|---|---|---|---|
| `sibling` | `Literal['cooc', 'core', 'chem']` | `"chem"` | Sibling | Radio |
| `ingredients` | `List[Literal['abalone', 'abalone_mushroom', 'absinthe', ... (1790 values)]]` | `["miso","basil","cumin"]` | Ingredients (1-5 polygons) | Dropdown |

**Returns (1)**

| type | UI label | component |
|---|---|---|
| `Dict(type: Literal['altair', 'bokeh', 'plotly', 'matplotlib'], plot: str)` |  | Plot |

### `/render_arithmetic_vector`

```python
client = Client("Kaikaku/epicure-explorer")
result = client.predict(
    sibling="core",
    positives=["miso"],
    negatives=["salt"],
    api_name="/render_arithmetic_vector"
)
```

**Parameters (3)**

| name | type | default | UI label | component |
|---|---|---|---|---|
| `sibling` | `Literal['cooc', 'core', 'chem']` | `"core"` | Sibling | Radio |
| `positives` | `List[Literal['abalone', 'abalone_mushroom', 'absinthe', ... (1790 values)]]` | `["miso"]` | Positives | Dropdown |
| `negatives` | `List[Literal['abalone', 'abalone_mushroom', 'absinthe', ... (1790 values)]]` | `["salt"]` | Negatives | Dropdown |

**Returns (1)**

| type | UI label | component |
|---|---|---|
| `Dict(type: Literal['altair', 'bokeh', 'plotly', 'matplotlib'], plot: str)` |  | Plot |

### `/render_cuisine_phylogeny`

```python
client = Client("Kaikaku/epicure-explorer")
result = client.predict(
    sibling="chem",
    api_name="/render_cuisine_phylogeny"
)
```

**Parameters (1)**

| name | type | default | UI label | component |
|---|---|---|---|---|
| `sibling` | `Literal['cooc', 'core', 'chem']` | `"chem"` | Sibling | Radio |

**Returns (1)**

| type | UI label | component |
|---|---|---|
| `Dict(type: Literal['altair', 'bokeh', 'plotly', 'matplotlib'], plot: str)` |  | Plot |

### `/render_cuisine_cosine_map`

```python
client = Client("Kaikaku/epicure-explorer")
result = client.predict(
    sibling="chem",
    api_name="/render_cuisine_cosine_map"
)
```

**Parameters (1)**

| name | type | default | UI label | component |
|---|---|---|---|---|
| `sibling` | `Literal['cooc', 'core', 'chem']` | `"chem"` | Sibling | Radio |

**Returns (1)**

| type | UI label | component |
|---|---|---|
| `Dict(type: Literal['altair', 'bokeh', 'plotly', 'matplotlib'], plot: str)` |  | Plot |

### `/render_slerp_trajectory`

```python
client = Client("Kaikaku/epicure-explorer")
result = client.predict(
    sibling="chem",
    seed="rice",
    direction="cuisine:South_Asian",
    max_theta=60,
    api_name="/render_slerp_trajectory"
)
```

**Parameters (4)**

| name | type | default | UI label | component |
|---|---|---|---|---|
| `sibling` | `Literal['cooc', 'core', 'chem']` | `"chem"` | Sibling | Radio |
| `seed` | `Literal['abalone', 'abalone_mushroom', 'absinthe', ... (1790 values)]` | `"rice"` | Seed | Dropdown |
| `direction` | `Literal['bitter_score/Aged and semi-hard cheeses', 'bitter_score/East Asian pungent aromatics and wild mushrooms', 'bitter_score/Mediterranean cheeses and peppers', ... (129 values)]` | `"cuisine:South_Asian"` | Direction | Dropdown |
| `max_theta` | `float` | `60` | Max θ (deg) | Slider |

**Returns (1)**

| type | UI label | component |
|---|---|---|
| `Dict(type: Literal['altair', 'bokeh', 'plotly', 'matplotlib'], plot: str)` |  | Plot |

### `/lambda_4`

```python
client = Client("Kaikaku/epicure-explorer")
result = client.predict(
    s="chem",
    api_name="/lambda_4"
)
```

**Parameters (1)**

| name | type | default | UI label | component |
|---|---|---|---|---|
| `s` | `Literal['cooc', 'core', 'chem']` | `"chem"` | Sibling | Radio |

**Returns (1)**

| type | UI label | component |
|---|---|---|
| `Literal['bitter_score/Aged and semi-hard cheeses', 'bitter_score/East Asian pungent aromatics and wild mushrooms', 'bitter_score/Mediterranean cheeses and peppers', ... (129 values)]` | Direction | Dropdown |

### `/neighbors`

```python
client = Client("Kaikaku/epicure-explorer")
result = client.predict(
    ingredient="Hello!!",
    sibling="Hello!!",
    k=5,
    api_name="/neighbors"
)
```

**Parameters (3)**

| name | type | default | UI label | component |
|---|---|---|---|---|
| `ingredient` | `str Required` | — | parameter_217 | Textbox |
| `sibling` | `str Required` | — | parameter_218 | Textbox |
| `k` | `float` | `5` | parameter_219 | Number |

**Returns (1)**

| type | UI label | component |
|---|---|---|
| `Dict[Any, Any]` | value_223 | Json |

### `/slerp`

```python
client = Client("Kaikaku/epicure-explorer")
result = client.predict(
    seed="Hello!!",
    direction="Hello!!",
    theta_deg=30,
    sibling="chem",
    k=5,
    api_name="/slerp"
)
```

**Parameters (5)**

| name | type | default | UI label | component |
|---|---|---|---|---|
| `seed` | `str Required` | — | parameter_217 | Textbox |
| `direction` | `str Required` | — | parameter_218 | Textbox |
| `theta_deg` | `float` | `30` | parameter_220 | Number |
| `sibling` | `str` | `"chem"` | parameter_226 | Textbox |
| `k` | `float` | `5` | parameter_219 | Number |

**Returns (1)**

| type | UI label | component |
|---|---|---|
| `Dict[Any, Any]` | value_223 | Json |

### `/arithmetic`

```python
client = Client("Kaikaku/epicure-explorer")
result = client.predict(
    positives=[],
    negatives=[],
    sibling="Hello!!",
    k=5,
    api_name="/arithmetic"
)
```

**Parameters (4)**

| name | type | default | UI label | component |
|---|---|---|---|---|
| `positives` | `Dict[Any, Any]` | `[]` | parameter_221 | Json |
| `negatives` | `Dict[Any, Any]` | `[]` | parameter_222 | Json |
| `sibling` | `str Required` | — | parameter_217 | Textbox |
| `k` | `float` | `5` | parameter_219 | Number |

**Returns (1)**

| type | UI label | component |
|---|---|---|
| `Dict[Any, Any]` | value_223 | Json |

### `/embed`

```python
client = Client("Kaikaku/epicure-explorer")
result = client.predict(
    ingredient="Hello!!",
    sibling="Hello!!",
    api_name="/embed"
)
```

**Parameters (2)**

| name | type | default | UI label | component |
|---|---|---|---|---|
| `ingredient` | `str Required` | — | parameter_217 | Textbox |
| `sibling` | `str Required` | — | parameter_218 | Textbox |

**Returns (1)**

| type | UI label | component |
|---|---|---|
| `Dict[Any, Any]` | value_223 | Json |

